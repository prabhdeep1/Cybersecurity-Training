# Credential storage directory

This directory is a placeholder for server-side credential storage. For now, the
site stores demo credentials in browser local storage. When a backend is added,
store hashed credentials and authentication configuration here.
